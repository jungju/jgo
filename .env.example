# Required when prompt optimization is enabled
# (jgo exec --optimize-prompt, or JGO_OPTIMIZE_PROMPT=true)
OPENAI_API_KEY=your_openai_api_key
MODEL=gpt-4.1-mini

# Optional (default: https://api.openai.com/v1)
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional execution transport (default: local)
JGO_EXEC_TRANSPORT=local

# Optional SSH override (used only when JGO_EXEC_TRANSPORT=ssh)
# JGO_SSH_USER=jgo
# JGO_SSH_HOST=localhost
# JGO_SSH_PORT=22

# Optional runtime
CODEX_BIN=codex
CODEX_REASONING_EFFORT=xhigh
JGO_LISTEN_ADDR=:8080
JGO_AVAILABLE_CLIS=aws,gh,kubectl
JGO_OPTIMIZE_PROMPT=false

# Optional provider fallback
# OPENWEBUI_API_KEY=
# OPENWEBUI_MODEL=
# LITELLM_API_KEY=
# LITELLM_MODEL=

# Optional cloud/k8s
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=ap-northeast-2
# GITHUB_TOKEN=
# KUBECONFIG=/work/.kube/config
